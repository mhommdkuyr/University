const API='/api/v1';async function load(){try{const r=await fetch(API+'/projects');if(!r.ok)return;const data=await r.json();document.querySelector('#project-list').innerHTML=(data.items||[]).map(p=>'<article><b>'+escapeHtml(p.title)+'</b><p>'+escapeHtml(p.description||'')+'</p></article>').join('')}catch(e){}}function escapeHtml(s){return String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}document.querySelector('#login-form').addEventListener('submit',async e=>{e.preventDefault();const f=new FormData(e.target);const status=document.querySelector('#status');try{const r=await fetch(API+'/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:f.get('email'),password:f.get('password')})});const d=await r.json();status.textContent=r.ok?'تم تسجيل الدخول بنجاح':'تعذر تسجيل الدخول: '+(d.detail||'تحقق من البيانات')}catch{status.textContent='تعذر الاتصال بالخادم'}});load();